{% extends 'admin_base.html.twig' %}

{% block title %}Duree{% endblock %}

{% block body %}


    <div id="container-main">

       <div class="menu">
        
            <a href="{{ path('duree_index') }}">retour à la liste</a>

            <a href="{{ path('duree_edit', {'id': duree.id}) }}">édition</a>
        
        </div>

        <div class="content">
        
            <h1>Duree</h1>

            <table class="table">
                <tbody>
                    <tr>
                        <th>Id</th>
                        <td>{{ duree.id }}</td>
                    </tr>
                    <tr>
                        <th>Mensualite</th>
                        <td>{{ duree.mensualite }}</td>
                    </tr>
                </tbody>
            </table>

            {{ include('duree/_delete_form.html.twig') }}
        
        </div>


    </div>
   
{% endblock %}
